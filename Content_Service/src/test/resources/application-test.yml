spring:
  liquibase:
    enabled: false
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:h2:mem:testdb  # или отключить полностью
    driver-class-name: org.h2.Driver
    username: sa
    password:
